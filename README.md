# Statistics_Website
A simple statistics website in which you can enter your marks and view statistical reports such as average, highest marks, marks required to reach the goal etc

To run the code you need to install libraries such as streamlit, pandas and plotly.express


To install the libraries run the following commands

pip install streamlit
pip install pandas
pip install plotly.express
